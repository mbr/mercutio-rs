[package]
name = "mercutio"
description = "IO-less MCP server library"
version = "0.1.0"
edition = "2024"

[dependencies]
rust-mcp-schema = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
